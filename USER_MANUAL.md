# 海外客 (AutoLeadGen) 使用文档

欢迎使用 **海外客 (AutoLeadGen)** —— 您的智能外贸获客系统。本系统集成了全网客户搜索、精准决策人邮箱挖掘、AI 个性化开发信撰写以及自动化邮件营销（EDM）功能，致力于帮助出海企业实现高效、智能的客户开发。

## 核心功能概览

1. **AI 对话助手**: 您的智能业务助理，支持自然语言对话交互、多格式文件解析和全自动客户搜索与挖掘指令。
2. **客户画像配置**: 定义和管理多维度的目标客户群体画像（行业、国家、职位、关键词等）。
3. **客户库管理**: 统一沉淀潜在客户（Leads）数据，跨工作流自动去重。
4. **自动化工作流**: 建立从搜索、挖掘到发送邮件的全自动管线。
5. **发件邮箱配置**: 支持多邮箱（SMTP/IMAP）轮换发送及收件箱监控。
6. **客户回信跟踪**: 自动拉取客户回复，并智能生成跟进邮件草稿。

---

## 模块详细说明及操作指南

### 1. 发件邮箱配置 (必备基础)

在开始任何客户开发之前，您需要配置至少一个用于发送邮件和接收回信的邮箱账号。

*   **操作路径**: 左侧菜单栏 -> 💼 WORKSPACE -> **📧 发件邮箱配置**
*   **操作步骤**:
    1. 点击右上角 **“+ 添加邮箱”**。
    2. 填写您的邮箱地址和发件人显示名称（如：John from Acme）。
    3. 配置 **SMTP 服务器**（用于发信），例如 `smtp.gmail.com`，端口通常为 `465` (SSL) 或 `587` (TLS)。输入您的邮箱密码或授权码（App Password）。
    4. 配置 **IMAP 服务器**（用于监控回信），例如 `imap.gmail.com`，端口通常为 `993`。
    5. 点击 **“测试发送”** 和 **“测试收信”**，确保配置无误后点击 **“保存配置”**。
*   *提示：系统支持添加多个邮箱，在工作流中可实现多邮箱轮换发送，降低封号风险。*

### 2. 客户画像配置 (Persona)

精准定位目标客户，是高效开发的第一步。

*   **操作路径**: 左侧菜单栏 -> 💼 WORKSPACE -> **👥 客户画像配置**
*   **操作步骤**:
    1. 点击 **“+ 新建画像”**。
    2. **画像名称**: 为此画像命名（如：欧洲 Padel 零售商）。
    3. **目标行业 & 地区**: 例如 “Sports Equipment”, “Europe, Spain, Italy”。
    4. **目标职位**: 决策者职位，如 “CEO, Founder, Buyer”。
    5. **搜索关键词 & 排除词**: 用于指导系统在全网搜索时使用哪些核心词，以及避开哪些词（如排除 `manufacturer`）。
    6. **写信指导 (Prompt)**: 告诉 AI 针对这群客户如何撰写邮件（例如：强调我们产品的性价比优势，提供免费打样服务等）。
    7. 点击 **“保存画像”**。

### 3. 客户库管理 (Client Pools)

客户库用于沉淀您搜索到的线索，方便分类管理和后续跟进。

*   **操作路径**: 左侧菜单栏 -> 💼 WORKSPACE -> **📁 客户库管理**
*   **操作步骤**:
    1. 点击 **“+ 新建客户库”**。
    2. 填写客户库名称及描述。
    3. **排除域名**: (重要) 输入您想过滤掉的域名（如竞争对手的域名、已合作客户的域名等），系统在入库时会自动过滤。
    4. 保存后，点击客户库卡片可进入详情页，查看已入库的客户列表，支持按状态过滤，并可 **导出 CSV**。

### 4. 自动化工作流 (Workflows)

工作流是整个系统的自动化引擎，将画像、客户库和邮箱串联起来，实现自动外推。

*   **操作路径**: 左侧菜单栏 -> 💼 WORKSPACE -> **⚙️ 自动化工作流**
*   **操作步骤**:
    1. 点击 **“+ 新建工作流”**。
    2. 设定工作流名称。
    3. 您可以选择 **绑定客户画像**，系统会自动填入该画像的搜索参数和写信指导；您也可以手动修改参数。
    4. **绑定客户库**: 选择此工作流挖掘到的客户存入哪个客户库中进行去重。
    5. **绑定发件邮箱**: 勾选一个或多个发件邮箱，系统将使用这些邮箱轮换发送邮件。
    6. **高级发送策略**:
        *   **单日最高发送量**: 控制每天发送上限。
        *   **发信间隔**: 设置随机发信间隔，模拟真人操作。
        *   **自动跟进**: 勾选后，若收到客户回复，AI 会自动为您发送下一封跟进邮件（否则仅生成草稿供您人工审核）。
    7. 启动工作流后，可在页面顶部的 **“📜 运行日志 (实时)”** 查看底层引擎的运行状态。

### 5. 客户回信跟踪 & 邮件发送记录

实时监控您的邮件营销效果和客户反馈。

*   **操作路径**:
    *   **发送记录**: 点击 **📊 邮件发送记录**，查看所有已成功发送出去的邮件时间、收件人、主题等信息。
    *   **回信跟踪**: 点击 **📨 客户回信跟踪**。
*   **处理回信**:
    1. 在回信跟踪页面，点击 **“🔄 刷新收件箱”**，系统会通过配置的 IMAP 拉取最新邮件。
    2. 当检测到客户回信时，系统会自动将该客户状态更新为 `replied`，并展示在时间线上。
    3. 如果您在工作流中未开启自动跟进，您可以在此查看 AI 为您生成的跟进回复草稿，并进行人工干预和发送。

### 6. ✨ AI 对话助手 (核心体验)

您可以直接与 AI 对话，下发指令进行一站式操作。

*   **操作路径**: 左侧菜单栏 -> 🤖 ASSISTANT -> **✨ AI 对话助手**
*   **典型使用场景**:
    1. **自然语言搜索**: 在对话框输入指令，如：“帮我搜索欧洲的 Padel 设备销售商，寻找他们的 CEO，并给他们发一封介绍产品的邮件。”
    2. **上传文件解析**: 点击输入框左侧的 📁 图标，上传您的产品介绍、客户画像需求文档 (.txt, .pdf, .docx, .csv 等)，AI 会自动读取内容并生成对应的搜索和开发策略。
    3. **多会话管理**: 左侧菜单栏下方支持创建多个会话 (New Chat)，方便管理不同的开发项目。

---

## 常见问题 (FAQ)

**Q1: 系统提示 "请先在侧边栏配置发件邮箱" 怎么办？**
A: 请前往 “发件邮箱配置” 页面，按照步骤添加至少一个可用的 SMTP 邮箱账号，并测试通过。

**Q2: 如何防止发出的邮件被判定为垃圾邮件 (Spam)？**
A: 
1. 在工作流中设置合理的“发信间隔”（建议 60-300 秒）。
2. 配置多个发件邮箱进行轮换发送。
3. 确保您的发件邮箱域名已正确配置 SPF、DKIM 和 DMARC 记录。

**Q3: 客户回复了邮件，系统怎么知道？**
A: 请确保在“发件邮箱配置”中正确填写了 IMAP 信息。系统后台服务会定时（默认每5分钟）检查收件箱，或者您可以手动在“客户回信跟踪”页面点击刷新。

## 技术支持
如需系统部署帮助或遇到环境依赖问题，请参阅根目录下的 `DEPLOYMENT.md` 文件。
