# AutoLeadGen 产品体验审计报告

> 审计日期：2026-05-29 | 审计范围：前端 14 页面 + 后端 11 路由模块 + 7 核心服务

---

## 总体评分：6.2 / 10

| 维度 | 评分 | 说明 |
|------|------|------|
| 核心功能完整度 | 7.5/10 | 搜索→评分→起草→发信→收信→回复闭环完整，但缺少 Lead 独立管理 |
| 国际化 (i18n) | 3/10 | 14 个页面中仅 6 个有翻译，8 个完全硬编码英文 |
| 错误处理与反馈 | 5/10 | sonner toast 已引入但各页面不一致，confirm() 仍大量使用 |
| 安全性 | 5/10 | 6 个 API 端点缺少所有权检查，admin 判断依赖客户端 localStorage |
| 性能 | 6/10 | 无分页、内存加载全量数据、dashboard 趋势图全量查询 |
| 代码一致性 | 4/10 | 中英文 toast 混用、i18n 方案不统一、Pydantic schema 缺失 |

---

## 一、用户旅程审计（按操作流程）

### 1.1 登录页 → Dashboard

**问题：**
- [严重] `placeholder="admin"` 暴露管理员用户名，生产环境应删除
- [轻微] `<html lang="en">` 硬编码，中文用户时 screen reader 不友好
- [轻微] "Contact Sales" 链接 `<a href="#">` 无实际跳转

**做得好的：**
- 登录表单有 loading 状态和错误提示
- 401 自动跳转登录页

### 1.2 Dashboard 首页

**问题：**
- [严重] **System Status 面板全部硬编码为 "Online"**，不是真实健康检查。用户看到绿灯以为系统正常，实际可能 outbound engine 已挂
- [中等] 趋势图数据全量加载到 Python 内存（14 天所有 lead + email），数据量大时 OOM
- [中等] Quick Start 引导永远显示，对老用户无意义
- [轻微] 加载态只有 "Loading analytics..." 文字，无骨架屏

**做得好的：**
- KPI 卡片 + 趋势图 + 每日报告三区块布局合理
- 有 i18n 翻译

### 1.3 创建工作流 (Workflow)

**问题：**
- [严重] **整个页面零 i18n**，30+ 表单字段全部英文，中国用户无法使用
- [严重] **表单极长**（30+ 字段），无分步向导、无折叠分组，信息过载
- [中等] Playbook 预设选择只出现在创建时，跳过之后无法回头选
- [中等] "跳过，直接从空白创建 →" 硬编码中文，与页面英文不一致
- [中等] 无表单校验错误提示（仅 HTML `required` 属性）
- [中等] 引擎日志对话框写的是 "Real-time" 但不自动刷新，必须手动点
- [轻微] 切换状态按钮 loading 显示中文 "切换中..."

**做得好的：**
- Playbook 预设 + 角色自动填充 + AI 关键词生成，功能设计好
- 工作流卡片展示 lead 统计（总数/可触达/缺邮箱/低分），信息密度合理
- 搜索源、LinkedIn/WhatsApp 开关可配置

### 1.4 客户库 (Client Pools)

**问题：**
- [严重] **删除确认用原生 `confirm('确定要删除这个客户库吗？')`**，不可定制样式，不可翻译
- [中等] i18n 使用自定义 `txt(en, zh)` 函数而非全局 `t()`，与项目其他页面不一致
- [中等] 日期格式化硬编码 `'zh-CN'` locale，英文模式下仍显示中文日期
- [中等] Lead 详情弹窗一次加载 1000 条，无分页
- [轻微] 状态筛选按钮标签未翻译（"All", "Found", "Drafted"...）
- [轻微] CSV 导出失败时 `console.error(e)`，用户无感知

**做得好的：**
- Lead 详情弹窗功能丰富：状态标签、fit score、邮箱验证状态、评分、AI 研究简报
- CSV 导出功能实用

### 1.5 角色画像 (Personas)

**问题：**
- [严重] **零 i18n**，15+ 字段全部英文
- [严重] **无编辑功能**，只能创建和删除，改一个字段必须删了重建
- [中等] 删除确认用原生 `confirm()`
- [中等] 删除时不检查是否有 workflow 引用此 persona

**做得好的：**
- 15 个字段覆盖全面（行业、国家、角色、关键词、资格规则、文化备注等）

### 1.6 邮箱配置 (Emails)

**问题：**
- [严重] **零 i18n**
- [严重] **SMTP 密码明文传输**（POST body 中 `smtp_pass` 为明文 JSON）
- [中等] 删除确认用原生 `confirm()`
- [中等] SMTP user 字段自动从 email 填充但不可手动覆盖
- [轻微] 测试结果不持久化，刷新后重置

**做得好的：**
- SMTP/IMAP 一键测试功能，反馈及时
- 连接状态图标直观

### 1.7 回复管理 (Replies)

**问题：**
- [轻微] 无已读/未读视觉区分
- [轻微] AI 草稿仅存 React state，刷新丢失

**做得好的：**
- 完整 i18n
- sonner toast 正确使用
- "不感兴趣"意图识别 + 玫瑰色横幅提示
- AI 生成回复 → 编辑 → 发送流程顺畅

### 1.8 发信日志 (Email Logs)

**问题：**
- [严重] **零 i18n**
- [严重] **无分页**，全量加载所有日志
- [中等] summary 卡片 `?? 'null'` 显示字符串 "null" 而非 "—"
- [中等] 无法按日期/状态/工作流筛选
- [中等] 后端 `deliverability_summary` 全量加载到内存计算，高危

**做得好的：**
- Deliverability 摘要卡片（Sent / Failed / Suppressed）+ 风险域名列表设计合理
- 日志表格列（时间/方向/路由/Lead/主题）信息完整

### 1.9 全渠道设置 (Settings)

**问题：**
- [严重] **零 i18n**，toast 硬编码中文
- [严重] **Unipile Webhook 无签名验证**，任何人都可以发送伪造 webhook
- [中等] 无断开/重连按钮
- [中等] 错误 toast 显示 `[object Object]`（`toast.error('网络错误，无法发起连接: ${e}')`）
- [中等] 连接按钮无 loading 状态

**做得好的：**
- OAuth 流程基本可用

### 1.10 AI 助手 (Agent)

**问题：**
- [中等] 零 i18n，示例 prompt 硬编码中文
- [中等] 对话历史不持久化，刷新即丢失
- [中等] 清除对话用原生 `confirm()`
- [中等] 后端 `run_agent_loop` 同步阻塞调用，FastAPI event loop 可能卡住
- [轻微] Markdown 渲染为自定义实现，不支持表格、任务列表等

**做得好的：**
- 意图识别 + 启发式预过滤（防止 LLM 编造公司列表）
- 45 秒安全超时
- 加载动画流畅

### 1.11 用户管理 (Users)

**问题：**
- [严重] **零 i18n**，toast 全部硬编码中文
- [中等] 密码明文传输
- [中等] 无编辑功能，不能改用户名/显示名/角色
- [中等] 客户端 `isAdmin` 判断不安全（从 localStorage 读取）
- [轻微] 删除确认用原生 `confirm()`

**做得好的：**
- 自保护（不能禁用/删除自己）
- 管理员角色区分

---

## 二、安全审计（按严重程度）

### 致命 (P0)
| # | 问题 | 位置 |
|---|------|------|
| 1 | Unipile Webhook 无签名验证，可伪造 | `routers/channels.py:94` |
| 2 | `PUT/DELETE /api/leads/{id}` 无所有权检查 | `routers/leads.py:21,35` |
| 3 | LinkedIn/WhatsApp 发送无用户隔离 | `routers/channels.py:162,232` |
| 4 | admin 判断依赖客户端 localStorage（可伪造） | `layout.tsx:59-64` |

### 严重 (P1)
| # | 问题 | 位置 |
|---|------|------|
| 5 | SMTP 密码明文传输 | `routers/email_accounts.py:12` |
| 6 | 用户密码明文传输 | `routers/auth.py:70` |
| 7 | 登录无速率限制 | `routers/auth.py` |
| 8 | `placeholder="admin"` 暴露用户名 | `login/page.tsx:85` |
| 9 | `GET /api/workflows/playbook-presets` 无认证 | `routers/workflows.py:13` |

---

## 三、i18n 完整度矩阵

| 页面 | i18n 状态 | 方法 |
|------|----------|------|
| Landing `/` | 基本完成 | `t()` |
| Login `/login` | 完成 | `t()` |
| Dashboard `/dashboard` | 基本完成 | `t()` |
| Layout (sidebar/header) | 完成 | `t()` |
| Workflows | **零 i18n** | — |
| Pools | 部分 (自定义 `txt()`) | `txt(en, zh)` |
| Personas | **零 i18n** | — |
| Emails | **零 i18n** | — |
| Replies | 完成 | `t()` |
| Email Logs | **零 i18n** | — |
| Settings | **零 i18n** | — |
| Agent | **零 i18n** | — |
| Sandbox | **零 i18n** | — |
| Users | **零 i18n** | — |
| Error Boundary | **零 i18n** | — |

**8/14 页面零 i18n，4 个页面中英混用。**

---

## 四、交互一致性问题

### 4.1 toast 语言混用
- Workflows: toast 全部中文
- Settings: toast 全部中文
- Users: toast 全部中文
- Emails: 中英混用
- Replies: 英文（正确）
- Pools: 中文

### 4.2 `confirm()` 使用（6 处）
全部应替换为自定义 Dialog 组件：
- `confirm('确定要删除这个客户库吗？')` — pools
- `confirm('确定要删除这个邮箱吗？')` — emails
- `confirm('确定要删除这个工作流及其所有线索吗？')` — workflows
- `confirm("Are you sure you want to clear the chat history?")` — agent
- `confirm('确定要删除这个画像吗？')` — personas
- `confirm('确定要删除这个账号吗？...')` — users

### 4.3 `console.error()` 泛滥
39 处 `console.error()` 散布在各页面，生产环境应替换为结构化错误上报。

---

## 五、后端 API 质量

### 5.1 缺失的端点
- **无 `GET /api/leads/`** — 没有独立的 lead 列表端点，leads 只能通过 pool/workflow/replies 间接访问
- 无 `GET /api/workflows/{id}` — 没有单个 workflow 查询
- 无密码修改端点
- 无 unsubscribe 处理端点（但邮件中有 unsubscribe 链接）

### 5.2 数据完整性风险
- `DELETE /api/leads/{id}` 不级联删除 EmailLogs、MessageLogs、LeadBrief、LeadFeedback
- `DELETE /api/personas/{id}` 不检查 workflow 引用
- `DELETE /api/client_pools/{id}` 删除 leads 但不考虑 workflow 引用
- Webhook 创建账号硬编码 `user_id=1`

### 5.3 性能风险
- Email log deliverability summary 全量加载到内存
- Dashboard 分析全量加载 user workflow/lead IDs
- 多个端点返回无 Pydantic model 的裸 dict

### 5.4 代码质量
- `agent/chat` 同步阻塞调用 → 应改为 async
- 3 个服务独立硬编码 MiniMax LLM 默认值（应提取为共享 client）
- 多处 `float(os.environ.get(...))` 无 try/except

---

## 六、改进优先级建议

### 第一批（1-2 天）：用户体验致命伤
1. Workflows 页面 i18n（最核心页面，30+ 字段）
2. 替换全部 6 处 `confirm()` 为 Dialog 组件
3. 统一 toast 语言（全部走 `t()`）
4. 修复 System Status 硬编码 → 真实健康检查 API
5. 修复 `placeholder="admin"` 安全泄露

### 第二批（2-3 天）：API 安全补全
6. `PUT/DELETE /api/leads/{id}` 加所有权检查
7. Unipile Webhook 加签名验证
8. LinkedIn/WhatsApp 发送加用户隔离
9. Admin 判断改为后端验证
10. 登录接口加速率限制

### 第三批（3-5 天）：i18n 补全
11. Personas、Emails、Email Logs、Settings、Agent、Users 页面 i18n
12. Error Boundary i18n
13. `<html lang>` 动态切换

### 第四批（3-5 天）：功能补全
14. Personas 编辑功能
15. Email Logs 分页 + 筛选
16. Lead 独立列表端点 + 分页
17. Workflow 创建表单分步向导
18. 对话历史持久化

### 第五批（持续）：代码质量
19. 提取共享 LLM client
20. 级联删除修复
21. 性能优化（内存→SQL 聚合）
22. `console.error` → 结构化日志